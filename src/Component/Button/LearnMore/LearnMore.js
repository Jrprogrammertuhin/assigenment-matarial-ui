import React from 'react';

const LearnMore = (props) => {
    console.log(props.LearnMore.email)
    // const {name} = props.LearnMore;
    return (
        <div>
            {/* <h1>Name:{name}</h1> */}
        </div>
    );
};

export default LearnMore;